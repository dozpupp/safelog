# Comma-separated list of allowed CORS origins (e.g. https://safelog.hashpar.com,http://localhost:5173)
ALLOWED_ORIGINS=http://localhost:5173

# Secret key for deterministic PQC key generation (Must be set!)
SAFELOG_SECRET_KEY=
